/*package main

import (
	"fmt"
	"sort"
)

// 这里只说一下比较用的三个函数Ints,Float64s,Strings,更多的去网上看，比如二维切片的比较，map切片的比较，结构体切片的比较等等
func test_Ints() { //传入int切片，对int切片从小到大排序,与之相同的还有Float64s和Strings,都是传入切片然后排序
	s := []int{1, 3, 2, 4}
	sort.Ints(s)
	fmt.Printf("s: %v\n", s)
}

func main() {
	test_Ints()
}*/